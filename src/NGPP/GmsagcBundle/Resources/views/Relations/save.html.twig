{% extends "::base.html.twig" %}

{% block content %}
    <form
        {% if form.vars.value.id %}
            action="{{ path('ngpp_gmsagc_relations_save', {'id': form.vars.value.id}) }}"
        {% else %}
            action="{{ path('ngpp_gmsagc_relations_save') }}"
        {% endif %}
        method="post" {{ form_enctype(form) }}>
        <fieldset>
          <legend>{{ 'invoices.save'|trans }}</legend>
            {{ form_widget(form) }}
            <br>
            <input type="submit" class="btn btn-success" value="{{ 'action.save'|trans }}"/>
            <a href="{{ path('ngpp_gmsagc_relations') }}" class="btn">
                {{ 'action.cancel'|trans }}
            </a>
        </fieldset>
    </form>
{% endblock %}
